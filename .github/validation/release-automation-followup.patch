diff --git a/scripts/publish_release.ps1 b/scripts/publish_release.ps1
index 02b6de8..2768d9d 100644
--- a/scripts/publish_release.ps1
+++ b/scripts/publish_release.ps1
@@ -119,7 +119,7 @@ if (-not $Title) {
     $Title = "$Name $Tag"
 }
 
-$Head = (& git rev-parse --short HEAD).Trim()
+$Head = (& git rev-parse HEAD).Trim()
 if ($LASTEXITCODE -ne 0 -or -not $Head) {
     throw "Could not determine current commit."
 }
@@ -259,12 +259,12 @@ $ChangeSummaryText
 - 실행 파일: $($ExeItem.Name) ($($ExeItem.Length) bytes)
 - 압축 파일: $($ZipItem.Name) ($($ZipItem.Length) bytes)
 - 압축 해제 안내: README-실행안내.txt
-- SHA256: $ZipHash
+- ZIP SHA256: $ZipHash
 
 ## 검증
 
 - 검증 명령: scripts\verify_release.py --exe
-- 기준 커밋: $Head
+- 기준 커밋 SHA: $Head
 - 브랜치: $Branch
 "@ | Set-Content -LiteralPath $NotesPath -Encoding UTF8
 
diff --git a/tests/test_verify_release.py b/tests/test_verify_release.py
index 0306e3c..c85aeff 100644
--- a/tests/test_verify_release.py
+++ b/tests/test_verify_release.py
@@ -176,6 +176,16 @@ def test_field_verification_docs_match_current_graph_controls() -> None:
     assert "`max_pending_ping_count`" in text
 
 
+def test_publish_release_notes_include_traceable_zip_metadata() -> None:
+    text = (Path(__file__).resolve().parents[1] / "scripts" / "publish_release.ps1").read_text(encoding="utf-8-sig")
+
+    assert "git rev-parse HEAD" in text
+    assert "git rev-parse --short HEAD" not in text
+    assert "- ZIP SHA256: $ZipHash" in text
+    assert "- 기준 커밋 SHA: $Head" in text
+    assert "- 압축 파일: $($ZipItem.Name)" in text
+
+
 class _Signal:
     def __init__(self) -> None:
         self._callbacks = []

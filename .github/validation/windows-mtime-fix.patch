diff --git a/scripts/run_stability_soak_suite.py b/scripts/run_stability_soak_suite.py
index cabe933..c25e8b5 100644
--- a/scripts/run_stability_soak_suite.py
+++ b/scripts/run_stability_soak_suite.py
@@ -18,6 +18,7 @@ from scripts.soak_test import SOAK_PROFILES, evaluate_summary, parse_args as par
 
 
 DEFAULT_PROFILES = ("long4h", "long8h", "long24h", "ui10", "ui20", "ui50")
+SUMMARY_MTIME_GRACE_SECONDS = 1.0
 
 
 def main(argv: list[str] | None = None) -> int:
@@ -227,7 +228,7 @@ def load_latest_summary(output_dir: Path, *, since: float | None = None) -> dict
     candidates = []
     for path in output_dir.glob("soak_*_targets_*.json"):
         mtime = path.stat().st_mtime
-        if since is not None and mtime < since:
+        if since is not None and mtime + SUMMARY_MTIME_GRACE_SECONDS < since:
             continue
         candidates.append((mtime, path.name, path))
     candidates.sort()
diff --git a/tests/test_stability_soak_suite.py b/tests/test_stability_soak_suite.py
index 8f81939..d20467d 100644
--- a/tests/test_stability_soak_suite.py
+++ b/tests/test_stability_soak_suite.py
@@ -473,6 +473,17 @@ def test_soak_suite_loads_only_summaries_created_after_run_start(tmp_path) -> No
     assert summary["data"]["failures"] == []
 
 
+def test_soak_suite_allows_filesystem_mtime_grace_for_new_summary(tmp_path) -> None:
+    fresh = tmp_path / "soak_50_targets_20260101_020202.json"
+    fresh.write_text('{"failures": []}', encoding="utf-8")
+    os.utime(fresh, (149.5, 149.5))
+
+    summary = suite.load_latest_summary(tmp_path, since=150.0)
+
+    assert summary is not None
+    assert summary["path"] == fresh
+
+
 def test_soak_suite_run_profile_rejects_success_without_new_summary(tmp_path, monkeypatch) -> None:
     args = SimpleNamespace(
         python_executable="python",
